# EXP 1 : Linear and Circular Convolution

# AIM: 

# To perform Linear and Circular Convolution for two given sequence using SCILAB. 

# APPARATUS REQUIRED: 
PC installed with SCILAB. 

# PROGRAM (Linear Convolution): 

// Linear Convolution
clc;
clear;


// Input sequences
x = [1 1 2 1];
h = [1 2 3 4];

// Length for linear convolution
N = length(x) + length(h) - 1;

// Zero padding
x1 = [x, zeros(1, N - length(x))];
h1 = [h, zeros(1, N - length(h))];

// FFT of sequences
X = fft(x1, -1);
H = fft(h1, -1);

// Multiply in frequency domain
Yl = X .* H;

// IFFT to get linear convolution result
yl = real(fft(Yl, 1));

// Display result
disp(yl, "Linear Convolution Result:");



# PROGRAM (Circular Convolution): 

// Circular Convolution
clc;
clear;

// Input sequences
x1 = [1 -1 -2 3 -1];
x2 = [1 2 3];

// Length for circular convolution
N = max(length(x1), length(x2));

// Zero padding to length N
x1 = [x1, zeros(1, N - length(x1))];
x2 = [x2, zeros(1, N - length(x2))];

// FFT of sequences
X1 = fft(x1, -1);   // forward FFT
X2 = fft(x2, -1);   // forward FFT

// Multiply in frequency domain
Y = X1 .* X2;

// IFFT to get circular convolution result
y = real(fft(Y, 1));   // inverse FFT

// Display result
disp(y, "Circular Convolution Result:");

# OUTPUT (Linear Convolution): 
 1.   3.   7.   12.   12.   11.   4.
# OUTPUT (Circular Convolution): 
 8.  -2.  -1.  -4.  -1.
  "Circular Convolution Result:"

# RESULT: Thus, the Linear Convolution and Circular Convolution of the given sequences were successfully performed using SCILAB.
